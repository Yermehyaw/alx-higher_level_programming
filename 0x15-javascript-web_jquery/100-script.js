/**
 * Update the color of an elem regardless of the post. of <style>
 * in html file
 */
document.addEventListener('DOMContentLoaded', (event) => {
  const header = document.querySelector('header');
  header.style.color = '#FF0000';
});
